import { RetrieveIdPipe } from './retrieve-id.pipe';

describe('RetrieveIdPipe', () => {
  it('create an instance', () => {
    const pipe = new RetrieveIdPipe();
    expect(pipe).toBeTruthy();
  });
});
